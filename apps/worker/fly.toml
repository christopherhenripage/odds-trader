app = "odds-trader-worker"
primary_region = "iad"

[build]

[env]
  NODE_ENV = "production"

[processes]
  worker = "node dist/index.js"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  processes = ["worker"]
